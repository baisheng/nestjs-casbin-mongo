{
  "name": "nestjs-casbin-mongo",
  "version": "1.0.0",
  "main": "index.js",
  "author": "baisheng",
  "license": "MIT",
  "scripts": {
    "watch": "tsc -p ./tsconfig.build.json -w",
    "build": "rimraf lib && tsc -p ./tsconfig.build.json",
    "lint": "tslint --fix --project ./",
    "test": "jest --config ./jest.config.js",
    "ci": "yarn build"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "lib/**/*"
  ],
  "dependencies": {
    "casbin": "^5.9.1",
    "casbin-mongodb-adapter": "^1.2.3",
    "mongodb": "^3.6.9"
  },
  "devDependencies": {
    "@nestjs/common": "^7.6.18",
    "concurrently": "^6.2.0",
    "rimraf": "^3.0.2",
    "typescript": "4.1.5"
  }
}
